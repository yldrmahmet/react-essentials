export default function CoreConcept(props) {
  console.log("core concepts rendered");
  return (
    <li>
      <img src={props.image} alt={props.title} />
      <h3>{props.title}</h3>
      <p>{props.description}</p>
    </li>
  );
}